<main class="mx-auto max-w-5xl px-4 py-12">
    <header class="mb-8 border-b border-border pb-6">
        <h1 class="text-3xl font-semibold tracking-tight text-foreground"><%= __('categories') %></h1>
        <p class="mt-2 text-sm text-muted-foreground"><%= __('postCategoryTitle') %></p>
    </header>

    <%- partial('_widget/category-cloud') %>

    <% if (site.categories && site.categories.length > 0) { %>
    <%- partial('_widget/category-radar') %>
    <% } %>
</main>
